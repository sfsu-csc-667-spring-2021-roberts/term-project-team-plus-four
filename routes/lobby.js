/**
 * File to handle lobbies and lobby chat
 * Also handles dashboard
 * 
 */
 var express = require('express');
 const socketapi = require('../backend/socketapi');

 //Get list of public games
 const getPublicGames = () => {
  
}

//Join game
const joinGame = (gameCode) => {
    
}

//Join private game
const joinPrivateGame = (gameCode) => {
  
}

//Create and start new game
const createGame = () => {
  
}

//List of players inside the game
const getGamePlayers = (gameCode) => {
  
}

//Lobby chat stuff
const sendMessage = () => {
  
}
